% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weather.R
\name{weather}
\alias{weather}
\alias{get_weather_hourly}
\alias{get_weather_daily}
\title{Get Hourly/Daily Weather from KNMI Through Open-Meteo}
\usage{
get_weather_hourly(
  latitude = 53.22,
  longitude = 6.57,
  metrics = "temperature_2m",
  past_days = 30,
  forecast_days = 0,
  model = "knmi_seamless",
  timezone = "Europe/Amsterdam",
  api_url = "https://api.open-meteo.com/v1/forecast"
)

get_weather_daily(min_hour = 8, max_hour = 17, fun = base::mean, ...)
}
\arguments{
\item{latitude}{Latitude in decimal degrees (WGS84). Default: \code{53.22} (Groningen). Use negative values for the Southern Hemisphere.}

\item{longitude}{Longitude in decimal degrees (WGS84). Default: \code{6.57} (Groningen). Use negative values for locations west of Greenwich.}

\item{metrics}{Character vector of one or more hourly weather metrics to retrieve (default: \code{"temperature_2m"}). Multiple metrics can be specified, e.g. \code{c("temperature_2m", "wind_speed_10m")}. Common metrics include:
\itemize{
\item \code{"temperature_2m"}: air temperature at 2 m above ground (degrees Celsius)
\item \code{"relative_humidity_2m"}: relative humidity at 2 m (\%)
\item \code{"precipitation"}: rain + snow total (mm)
\item \code{"wind_speed_10m"}: wind speed at 10 m (km/h)
\item \code{"cloud_cover"}: total cloud cover (\%)
}

See the \href{https://open-meteo.com/en/docs/knmi-api}{KNMI API documentation} for the full list of available variables.}

\item{past_days}{Integer number of past days to include (default: \code{30}, range: 0--92).}

\item{forecast_days}{Integer number of forecast days to include (default: \code{0}, range: 0--16). The native KNMI model provides up to 2.5 days; beyond that, the seamless model falls back to ECMWF IFS.}

\item{model}{Character string specifying the weather model. One of
\code{"knmi_seamless"} (default, KNMI + ECMWF blend),
\code{"knmi_harmonie_arome_europe"} (5.5 km, Central & Northern Europe), or
\code{"knmi_harmonie_arome_netherlands"} (2 km, Netherlands & Belgium).}

\item{timezone}{Character string for the timezone of returned timestamps (default: \code{"Europe/Amsterdam"}).}

\item{api_url}{Base URL of the Open-Meteo API endpoint.}

\item{min_hour}{First hour of the day to include on a \code{0-23} scale. Default: \code{8}.}

\item{max_hour}{Last hour of the day to include on a \code{0-23} scale. Default: \code{17}.}

\item{fun}{Function to use for calculation, defaults to \code{\link[=mean]{mean()}}.}

\item{...}{Arguments passed on to \code{get_weather_hourly()}.}
}
\value{
A \link{data.frame} with columns:
\itemize{
\item \code{time}: \code{POSIXct} timestamp in the requested timezone
\item One column per requested weather variable, named as given in \code{metrics}
}
}
\description{
Retrieves hourly weather data from the \href{https://open-meteo.com/}{Open-Meteo API}
using the KNMI HARMONIE AROME weather model via the \verb{/v1/forecast} endpoint.
The KNMI model provides high-resolution forecasts (2 km for the Netherlands,
5.5 km for Europe) updated hourly. The \code{"knmi_seamless"} model (default)
blends KNMI HARMONIE AROME with ECMWF IFS beyond the 2.5-day KNMI horizon.
}
\examples{
\dontrun{
# temperature in Groningen over the past 30 days
get_weather_hourly()
get_weather_daily() # default: between 08:00-17:59


assen <- geocode("Assen", as_coordinates = TRUE, crs = 4326)
latitude(assen)
longitude(assen)
get_weather_daily(latitude = latitude(assen),
                  longitude = longitude(assen))


# wind speed with 3-day forecast
get_weather_hourly(metrics = "wind_speed_10m", past_days = 7, forecast_days = 3)

# multiple variables at once
get_weather_hourly(metrics = c("temperature_2m", "relative_humidity_2m"))

# high-resolution Netherlands-only model
get_weather_hourly(model = "knmi_harmonie_arome_netherlands",
                   past_days = 0,
                   forecast_days = 2)
}
}
